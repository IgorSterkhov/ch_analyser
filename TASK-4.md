1. когда работает модуль формитирования запросов, при их выводе, то такие слова как TYPE, TABLES, USER - форматируются в верхний регистр, надо оставлять исходное форматирование.
2. почеиу то не работает кнопка COPY в окне GENERATED QUERY, не копируется в буфер обмена, причем так происходит только когда приложение развернуто на удлаенном сервере, при тестировании локально на машине разработчика - все работает
3. процент занятого места, на верхнем блоке, в прогресс-баре, надо округлять до 1 знака после запятой
4. в query history, когда определяются список элементов для фильтров User и Kind, и их взаимосвязь, то надо смотреть полную историю запросов по таблице и генерировать отдельный запрос к query_log, с group by. Затем, когда пользователь выбирает эти фильтры, надо генерировать запрос к query_log с where по таблице и с учетом выбранных фильтров, и выбранной сортировки. также добавить кнопку refresh справа от фильтров, по нажати.ю на которую и обновлять содержимое таблицы, также обновлять надо при открытии вкладки query history. также надо добавить дроп даун список с количеством выводимых запросов
5. надо сделать так чтобы блок "Table Details" был скрыт по умолчанию, и по нажатию на таблицу в блоке "Tables" - выезжал справа, при повторном нажатии на эту же таблицу - коллапсировал вправо, а также имел блок с крестиком, чтобы его можно было скрыть принудительно (панель должна сколлапсировать вправо), и также надо добавить прямоугольный блок со стрелочкой, как у панели с подключениями.
6. надо добавить вкладку в детализацию по таблице "Flow", в которой показывать 3 блока: 
6.1. "Materialised view Flow", в котором показывать последовательность таблиц и материализованных представлений, в потоке данных через которые участвует данная таблица. смотреть надо в system.query_views_log с фильтром по этой таблице, разворачивать от исходной таблицы, до конечной. например есть таблица t2, и флоу с ее участием выглядит так: t1 > mv1 > t2 > mv2 > t3, где t - таблицы, а mv - материализованные представления, в этом случае надо показать полный флоу.
6.2. "Query Flow", в котором показать по истории запросов (system.query_log) цепочку переливки данных.
6.3. "Full Flow", в котором обьединить флоу из 6.1 и 6.2
