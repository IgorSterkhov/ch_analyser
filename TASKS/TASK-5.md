1. добавил ленивую загрузку для вкладок "query history" и "flow" (уже сделано, пункт добавлен для истории релизов)
2. на вкладке "query history" надо сделать
2.1. фильтры по User и Kind - в 2 строки, каждый фильтр в своей строке, иначе если миого пользователей, то фильтра по kind не видно так как он уехал вправо
2.2. если много пользователей, то можно их отрисовывать плиткой, в несколько строк, в одной строке плитки пусть будет 7 пользователей
2.3. надо добавить кнопке "сбросить все" у каждого фильтра, только название фильтра надо не словами, а каким то обозначением, типа иконки корзины. прт нажатии на кнопку у соответствующего блока фильтров, например по user, со всех user снимается выделение в блоке фильтров.
2.4. надо добавить переключатель "прямые запросы", рядом с остальными управляющими элементами. Если переключатель включен, то надо дополнительно фильтровать запросы по тексту запросы, проверять что название таблицы (без указания базы) находится в тексте запроса. По умолчанию переключатель должен быть включен. Если переключатель выключен, то в таблице с историей запросов надо добавить колонку "Direct", и выводить "+", если запрос прямой, то есть что название таблицы (без указания базы) находится в тексте запроса.
2.5. перенести кнопку с просмотром кода запроса к query log - надо таблицей, рядом с остальными управляющими элементами
3. счётчик ref в таблице таблиц и в колонках таблицы - лучше увеличивать когда таблица служит источником для другой таблицы (или колонка служит источником), а сейчас везде единицы проставились из-за того, что как минимум одна матвьюха заполняет таблицу (столбец), так что, даже если таких заполняющих матвьюх десять штук, счётчик ref всё-равно должен быть равен нулю, если из самой таблицы никакая другая не заполняется
4. надо поправить округление до 1 знака после запятой в проценте заполненности диска, который высвечивается на полоске прогресс бара:  ![alt text](.local/photo_2026-02-19_20-21-16.jpg)
