дальнейшие требования и доработки:
1. не скроллятся таблицы, когда над ними курсор мыши, и крутится колесико
2. когда открываю справку - она тоже не скроллится мышкой
3. last_inserts надо переделать, надо реализлвать двойную логику. Если на сервере включен system.part_log, то надо брать из него 
```
select database, table, event_time
from system.part_log
where event_type ='NewPart'
order by event_time desc
```
это самый универсальный способ. Но если не включен - тогда надо брать последнюю дату из system.query_log (здест видно как вставляют данные обычные запросы) и system.query_views_log (здесь видно как вставляют данные материализованные представления).
4. надо добавить отметку что таблица реплицирована - в список таблиц. Причем смотреть надо не по движку таблицы, а по такому запросу:
```
select *
from system.replicas
where length(replica_is_active) > 1
```
это получше чем смотреть на движок - бывает, что движок Replicated, а таблица ток на одном хосте создана.
5. надо добавить - в список таблиц показатель references_cnt - кол-во упоминаний таблицы в разных DDL в базе. Реальный список можно получить только путем сканирования ддл из системных таблиц. Например из таблицы никто не селектит, но она служит источником для матвьюшки
или никто не селектит, но она служит источником для словаря или из словаря никто не селектит, но словарь используется в ALIAS dictGet какой-то таблицы и если ссылок больше, чем ноль - надо чтобы чтобы можно было в выпадающем окне, или во всплываюшем окне, открыть и скопировать перечисление сущностей, ссылающихся на эту таблицу.
5.1 такую же references_cnt добавить отдельно для каждой колонки в детализации по колонкам, чтобы можно было понять, какими колонками пользуются, а какими нет.
6. надо для каждой таблицы в списке таблиц видеть TTL, если есть в отдельной колонке.
7. надо добавить кнопку "</>" в блок с таблицами, как в блоке по колонкам, чтобы видеть каким запросом получена информация.
8. в блоке по колонкам, кнопка "</>" должна показывать запрос с учетом установленных фильтров по пользователю и kind.