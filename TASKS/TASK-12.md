Этап 1. Доработки по экрану "All Servers":
1. Всплывающая подсказка при наведении курсора на график с таблицами - не помещается на экран, когда таблиц много, в идеале если в подсказке будет видно только те таблицы, график по которым непосредственно под курсором. Иначе сделаь так чтобы подксказка располагалась справа по центру от курсора мыши, а не справа сверху от курсора, и уходила вверх за границу окна, как сейчас.
2. сами таблицы в легенде графика таблиц лучше расположить по вертикали справа, друг под другом, а не горизонтально, где их приходится прокручивать горизонтальным скроллом.
3. не хватает кнопок "скрыть все" и "выбрать все" в легенде, сейчас получается только поштучно убирать видимость по таблицами в графике. 
4. Предлагаю сделать так, чтобы при нажатии на строку в левой таблице таблиц, график отрисовывался по этой таблице.
5. В графике по серверам, слайдер для оси Х на самом деле не нужен. Давай сделаем выпадающий список над графиком, с количеством последних дней который он будет показывать (30, 60, 90, 180, 365), без возможности отзумить в любую сторону. А то этот слайдер наезжает на таблицы и на график.

Этап 2. Доработки по экрану "by Server Details":
1. В таблице таблиц, в значок R (для реплицированных таблиц) надо поменять на➕ или ➖(плюс - голубым, а минус - приглушённым серым, как в ref-ах), а то никак не привыкну - всё кажется, что это - кнопка "обновить"
2. для таблицы на вкладке "Users" 
    1) не нужен собственный скролл. достаточно скролла на уровне всей страницы.
    на скрине сейчас три скролла. ими трудно управлять одновременно - с одним было бы легче. на остальных вкладках так же. мб у всех элементов на странице нужно убрать фиксированный height.
    2) строку заголовка таблицы с именами колонок (position: fixed или position; sticky). 
    3) нужен ещё дриллдаун по юзеру (такой же как на вкладке Tables) - в виде боковой панели с детализацией по пользователю. Надо 
    - увидеть его запросы за выбранный интервал по убыванию event_ts, 
    - выбрать из них только неудачные или удачные, 
    - выбрать тип запроса: create-ы, insert-ы, и другие. 
    - и возможность отобразить в двух вариантах: либо все запросы as is, либо сгруппировать по normalized_query_hash и увидеть по хешам статистику - количество, количество с ошибками, последняя дата, были ли ошибки (и показывать последнюю ошибку при клике на ячейку)
3. строку заголовка таблицы с именами колонок на всех вкладках хорошо бы закрепить 
(position: fixed или position; sticky)
4. было бы хорошо добавить лоадеры! я переключаю интервал query_log на 1y или 7d - и ничего не происходит. и трудно понять, то ли запрос долго выполняется, то ли что-то сломалось в бэкенде.



