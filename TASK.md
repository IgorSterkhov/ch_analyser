Задача: Разработать приложение для анализа данных ClickHouse с двумя интерфейсами — веб (Nice GUI на Python) и десктопный (Tkinter).

📚 Функциональные требования

1. Управление подключениями:
⦁ Возможность добавлять новые подключения к ClickHouse (хост, порт, пользователь, пароль, база данных)
⦁ Сохранение настроек подключений в файл .env локально
⦁ Выбор активного подключения из списка сохраненных
⦁ Редактирование и удаление существующих подключений

2. Анализ таблиц:
⦁ После подключения получить список всех таблиц в выбранной базе данных
⦁ Для каждой таблицы отобразить:
  ⦁ Название таблицы
  ⦁ Размер в форматированном виде (используя функцию formatReadableSize() в ClickHouse) — автоматически показывает GB, MB, KB в зависимости от размера
  ⦁ Время последнего SELECT-запроса (когда таблица последний раз опрашивалась)
  ⦁ Время последней INSERT-операции (когда последний раз производилась вставка данных)

3. Детальный анализ колонок:
⦁ Возможность "провалиться" в выбранную таблицу для просмотра детальной информации
⦁ Для каждой колонки отобразить:
  ⦁ Имя колонки
  ⦁ Тип данных
  ⦁ Тип сжатия (compression codec)
  ⦁ Размер колонки в форматированном виде

📚 Технические требования

Технологический стек:
⦁ Python 3.8+
⦁ Nice GUI для веб-интерфейса
⦁ Tkinter для десктопного интерфейса
⦁ Библиотека clickhouse-driver для подключения к ClickHouse
⦁ python-dotenv для работы с .env файлами

Запросы к ClickHouse:
⦁ Размер таблиц: SELECT table, formatReadableSize(sum(bytes)) as size FROM system.parts WHERE active GROUP BY table
⦁ Информация о последних запросах: использовать системные таблицы system.query_log
⦁ Информация о колонках: system.columns и system.parts_columns
⦁ Типы сжатия: system.columns (поле compression_codec)

Структура .env файла:
CLICKHOUSE_CONNECTION_1_NAME=Production
CLICKHOUSE_CONNECTION_1_HOST=localhost
CLICKHOUSE_CONNECTION_1_PORT=9000
CLICKHOUSE_CONNECTION_1_USER=default
CLICKHOUSE_CONNECTION_1_PASSWORD=password
CLICKHOUSE_CONNECTION_1_DATABASE=default


📚 Архитектура приложения

1. Модуль подключений — управление конфигурациями и сохранение в .env
2. Модуль ClickHouse клиента — выполнение запросов и получение данных
3. Модуль UI (веб) — интерфейс на Nice GUI
4. Модуль UI (десктоп) — интерфейс на Tkinter
5. Общая бизнес-логика — анализ данных, форматирование

Создай полнофункциональное приложение с обработкой ошибок, логированием и пользовательским интерфейсом с удобной навигацией.